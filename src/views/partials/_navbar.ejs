<nav class="navbar navbar-expand-lg bg-body shadow-sm">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="#" id="toggleSidebar">
                    <i class="bi bi-list"></i>
                </a>
            </li>
            <li class="nav-item d-none d-md-block">
                <a href="/home" class="nav-link">Home</a>
            </li>
            <li class="nav-item d-none d-md-block">
                <a href="#" class="nav-link">Contact</a>
            </li>
        </ul>

        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="bi bi-search"></i></a>
            </li>
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle d-flex align-items-center" data-bs-toggle="dropdown">
                    <img src="/img/avatar5.png" class="rounded-circle me-2" width="30" alt="User Image" />
                    <span class="d-none d-md-inline">
                        <% if (typeof user !== 'undefined' && user && user.nombre) { %>
                            <%= user.nombre %>
                        <% } else { %>
                            Usuario
                        <% } %>
                    </span>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li class="dropdown-header text-center">
                        <img src="/img/avatar5.png" class="rounded-circle mb-2" width="60" alt="User Image" />
                        <div>
                            <strong>
                                <% if (typeof user !== 'undefined' && user && user.nombre) { %>
                                    <%= user.nombre %>
                                <% } else { %>
                                    Nombre de Usuario
                                <% } %>
                            </strong><br />
                            <small>
                                <% if (typeof user !== 'undefined' && user && user.rol) { %>
                                    <%= user.rol %>
                                <% } else { %>
                                    Rol
                                <% } %>
                            </small>
                        </div>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li class="px-3 py-2">
                        <a href="#" class="btn btn-outline-secondary btn-sm w-100 mb-2">Perfil</a>
                        <a href="/logout" class="btn btn-outline-danger btn-sm w-100">Cerrar sesión</a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>